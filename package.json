{
  "name": "fantasy-hub",
  "version": "1.0.0",
  "description": "Fantasy football league hub",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
